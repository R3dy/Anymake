# Project runs and generated artifacts — never commit
PROJECTS/

# Runtime state
PHASE_STATE.md

# OS junk
.DS_Store
Thumbs.db

# Eval harness output — runs are large, reproducible, and machine-specific.
# report.json / report.html are the deliverables; archive them deliberately.
evals/runs/
